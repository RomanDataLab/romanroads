Add population-based city estimates and a 3-scheme color menu to the Roman roads interactive map. All work in `C:\02_TEMP\18_CODINGHARD_6\roman_roads`.

## Data sources (verified during planning)

1. **Hanson 2016 Areas table** — OxREP `Hanson2016_Areas_OxREP.csv`, 885 of our 1,388 cities have measured built-up areas (hectares), shared `Primary Key` merge.
2. **Hanson & Ortman 2017 population method** — convert area → population with the paper's published density values (fetch the JRSI supplement from figshare `rs.figshare.com/.../5576680` first; if per-city estimates are there, use them directly, else apply the paper's central density coefficient). Basis: the gazetteer's nominal 1st–2nd c. AD snapshot → same-era compliance for all 885.
3. **Reba, Reitsma & Seto 2016 (Scientific Data)** — digitized Chandler/Modelski per-city, per-year populations for the world's largest cities: `chandler.csv` (figshare 10.6084/m9.figshare.2059494), `modelskiAncient.csv` (10.6084/m9.figshare.2059497). Used to gap-fill cities missing from the Areas table (mostly majors — Rome, Alexandria, Antioch, Carthage…) with fuzzy name matching (City + OtherName + modern toponym). Take the entry whose year is nearest **AD 150** (±50 yrs window); store `estimate_year` per city and show it in the tooltip when outside the window. Cities with no scientific estimate (expected: most of the ~500 minor towns) → "population unknown", grey.

Output artifact: `data/city_populations.csv` (primary_key, population, estimate_year, source) — auditable, cached.

## Interactive map changes (`roman_roads_analysis.py`)

**Layer-control menu with three city color schemes** (three FeatureGroups, only #1 shown by default; user toggles in the menu):
1. **Empire ranking (default)** — current Barrington-rank colors, unchanged.
2. **Population** — 5 log classes: <5k / 5–15k / 15–50k / 50–150k / >150k, sequential palette, grey = unknown.
3. **Foundation nation** — categorical colors aggregating the founded-by data: Roman (post-conquest foundations & Roman founders), Greeks (tooltip keeps the tribe detail — Dorian/Ionian/etc.), Phoenician, Etruscan, Latin, other Italic, Egyptian, Celtic/Gallic, Iberian, Illyrian–Thracian, Hellenistic kings (Seleucus etc.), Unknown. ~11 classes, fixed palette.

**Dot size**: switch to population-based log scale where known (village → Rome), rank-based size as fallback for unknowns.

**Tooltip/popup**: add `Population: ~N (est. YEAR)` line after Established (where known).

**Legend**: one side panel listing all three schemes (rank colors / population classes / nations).

If the HTML grows past ~10 MB from tripling markers, secondary schemes get tooltips only (no popups) — noted as fallback.

## Static outputs

- `findings.md`: top-10 cities by population table (Rome should top it), coverage stats (885 area-based + N literature-filled), sources cited.
- Static maps keep rank coloring; adopt population-based dot sizing.

## Deploy cycle (established pattern)

Run pipeline (wiki/founder caches intact) → re-inject GA4 (RDT-RRDS-54) → commit, push to `RomanDataLab/romanroads` → verify live (population lines count, three layer groups present, GA4 intact).

## Honesty caveats

- Exact Areas CSV schema and the density coefficient get finalized at implementation (fetchers couldn't render the raw CSV in plan mode).
- Literature gap-fill will realistically cover only a few dozen famous cities; the rest stay "unknown" — matching the actual state of science.
- GDP/welfare works remain excluded per your earlier instruction.